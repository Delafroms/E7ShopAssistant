# E7SA Changelog

> 版本对应关系：Git tag `v1.0.0` ↔ `versionName 1.0` ↔ `versionCode 16`。
> 详细缺陷台账见 [docs/TEST_REPORT.md](docs/TEST_REPORT.md)（编号 RT-001 ~ RT-014）。

---

## v1.0.0 — 首个公开版本（versionCode 16）

### Added
- 双识别引擎：YOLO 视觉识别与传统识图（由「购买」按钮文本推导行结构 + 图标区颜色签名 + 文本校验）
- 双点击管线：AI 点击（YOLO 眼睛 + 独立决策状态机）与传统点击（文本锚点），两条管线互不回退
- 预算闸门：金币消耗上限、天空石（付费货币）上限、书签/奖牌持有上限
- 悬浮任务面板（可拖拽、三套主题结构）、事件日志、会话统计
- 三套主题（Steam 深色卡片 / Steam OLED 纯黑 / Blue Archive 明亮学院）与完整中英文
- 记录页与会话历史、诊断工具（原始帧采集、回归台）
- 可选的 WebDAV 记录同步（用户自备服务器与凭据；不配置则不联网）

### Changed
- 顶部标识不再内置官方素材，改为「文字标题（默认）/ 自定义图片」（见下方 Security）
- 签名密钥更换为正式发布密钥，`versionCode` 由 15 提升到 16

### Fixed
- **RT-001** 同一行的确定性失败被无限重试 → 新增封顶预算（`VERIFY_FAIL`）
- **RT-002** 点击未生效却被标记为「已处理」，导致整屏漏买
- **RT-003** 预算记账被验证门控 → 上限永不触发，可无限消耗付费货币
- **RT-004** 原生层 use-after-free（运行中打开装备评分页会导致进程崩溃）
- **RT-005** 模型加载失败无法被检测（表现为「识别 0 结果但日志说模型正常」）
- **RT-006** Shizuku 一键授权通道因 R8 移除 provider 而失效
- **RT-007** 静默改写全设备级无障碍安全开关
- **RT-008** WebDAV 同步恒返回 409（云端目录从未创建；服务端对不存在的集合返回 409 而非 404）
- 主线程磁盘 I/O、评分引擎下标越界、悬浮窗销毁未释放引用、服务解绑后仍显示「已启用」等若干健壮性问题

### Security
- 凭据（WebDAV 账号与应用密码）独立存储，且从系统备份与设备迁移规则中排除
- 移除内置官方素材（版权原因）；启动图标为矢量图形
- 无障碍服务订阅范围限定为游戏包名；不再写入全局无障碍安全开关（改为设置项显式授权）

### Testing
- 单元测试 **115 / 115** 通过（含预算闸门语义、时序常量金值、评分引擎）
- 决策状态机整机测试：传统管线（钱路径 / 漏买回归 / fail-closed）与 AI 管线（模型缺失必须报错退出、零感知不盲点、停止即退）
- 红队用例 5 条（售罄行不点、假购买按钮不点、点错行立即放弃、价格歧义封顶、弹窗不关闭不写假成功）
- 真机验证：安装、启动、界面渲染、无障碍绑定、WebDAV 上传（真实云服务）
- **未进行**：整夜连续挂机、多机型、权限撤销与服务回收、长时间稳定性

### Known Issues
- RT-004 / RT-005 已修复但**未在真机复现验证**
- RT-009 应用更新后需重新开启无障碍服务（平台行为，非缺陷）
- RT-010 主题布局仍存在重复代码（技术债，不影响功能）
- RT-012 识别准确率受模型与画面条件限制（极端分辨率、UI 缩放、遮挡、动画中间帧）
- RT-013 自动化操作可能违反游戏服务条款，**存在账号风险**
- RT-014 需要无障碍服务与（可选的）安全设置写入权限

### Compatibility
- Android 11+（minSdk 30），arm64-v8a / armeabi-v7a
- 仅在 OnePlus PLR110 / Android 16 / ColorOS 上完成真机测试

### 升级注意
- 本版本更换了签名密钥：从早期调试签名版本升级**必须先卸载**；卸载会清除本机配置与云同步凭据
  （统计数据可先通过 WebDAV 上传，再在新版本中重新填写凭据下载）
- 更新后如提示「无障碍服务未开启」，请重新开启一次

---

## 未发布（Unreleased）

### In Progress
- **GitHub Actions**：首次运行失败 —— CI 环境未接受 `ndk;29.0.13113456` 的 SDK 许可证
  （`LicenceNotAcceptedException`，失败发生在配置阶段，单元测试尚未开始）。
  已修正为**只跑单元测试与 Lint**，并用 `-PskipNative=true` 跳过原生层配置。
- **CI 不构建 APK**：原生依赖（ncnn / opencv-mobile 预编译目录，约 146MB）未入库，
  见 `docs/BUILD.md`；本地完整构建需按该文档准备原生依赖。
- Gradle Wrapper 尚未入库（当前需自行准备 Gradle 8.11.1）

### Planned
- 主题布局层去重（当前仅完成逻辑层与共用弹窗的抽取）
- 原生层崩溃路径的真机复现验证（RT-004 / RT-005）
- 识别层端到端自动化（当前依赖人工回归台样本）
- 更多机型与分辨率适配验证
