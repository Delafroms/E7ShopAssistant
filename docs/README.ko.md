# E7SA — E7 Shop Assistant (E7 상점 도우미)

> **위험 고지**: 이 도구는 접근성 서비스를 통해 『에픽세븐』을 자동 조작합니다.
> **게임 이용약관을 위반할 수 있으며 계정 위험이 있습니다**. 사용에 따른 책임은 사용자에게 있으며,
> 이 프로젝트는 Smilegate와 아무런 관련이 없습니다.

E7SA는 『에픽세븐』 **비밀 상점**을 위한 보조 도구입니다: 스크린샷 → 인식 → 판단 → 탭.
목표 상품을 자동으로 구매하고, 살 것이 없으면 상점을 새로고침합니다.

- 언어: [简体中文](docs/README.zh-CN.md) · [English](docs/README.en.md) · [日本語](docs/README.ja.md) · [한국어](docs/README.ko.md)

## 개요

비밀 상점은 새로고침마다 6칸만 표시되고 목표 상품의 등장 확률은 낮습니다(칸당 서약서표 약 0.66%,
신비 메달 약 0.17%). 수동 새로고침은 부담이 크므로 "보기 → 판단 → 탭" 과정을 자동화합니다.
목표는 다음 세 가지뿐입니다.

1. **구매 누락 0** — 살 수 있는 목표를 보면 반드시 구매;
2. **오구매 0** — 근거가 부족하면 잘못 탭하는 대신 아무것도 하지 않음;
3. **예산 통제** — 골드와 하늘석(유료 재화)에 상한을 둠.

**개발 단계**: v1.0.0은 첫 공개 버전이며 **개인 프로젝트**입니다(상업용 제품이 아닙니다).
**테스트 완전성**: 단위 테스트와 적대적 테스트는 시뮬레이션 계층에서 통과했습니다. 실기기 검증은 설치,
실행, UI, 클라우드 동기화만 해당합니다. **장시간 연속 실행은 검증되지 않았습니다**("테스트 상태" 참조).

## 구현된 기능

- **두 개의 인식 엔진**: YOLO 시각 인식 / 전통적 CV("구매" 버튼 텍스트로 행 구조 추정 + 아이콘 영역 색상 서명 + 텍스트 검증)
- **두 개의 독립 탭 파이프라인**: AI 탭(YOLO를 눈으로 + 전용 의사결정 상태 머신)과 전통 탭(텍스트 앵커). 서로 대체 동작하지 않습니다
- **예산 게이트**: 골드 상한, 하늘석 상한, 서약서표/메달 보유 상한. 도달 시 중지
- **대기 전력 관리**: 실행 중 화면 켜짐 유지, 작업 완료 시 자동 화면 잠금(설정 가능)
- **플로팅 작업 패널**: 상태, 단계, 통계, 이벤트 로그, 시작/일시정지/중지(드래그 가능)
- **기록**: 세션 기록, 누적 구매와 소비
- **진단 도구**: 원본 프레임 캡처, 인식 회귀 벤치(버전 표시 길게 누르기)
- **세 가지 테마**(Steam 다크 카드 / Steam OLED 블랙 / Blue Archive 라이트)와 중국어·영어 UI
- **선택적 클라우드 동기화**: WebDAV(사용자 서버와 자격 증명). 설정하지 않으면 네트워크 통신이 없습니다

*Experimental(사용 가능하나 검증 중)*: 슬립 모드(전체 프레임 인식, 느리지만 안정적), 사용자 배경 이미지와
상단 타이틀 이미지, 테마 전환 이징 선택.

## 아키텍처

```
스크린샷(접근성 서비스)
  → 전처리(확대/축소, 해상도 비의존 샘플링 윈도)
  → 장면 판정(상점 목록 / 구매 대화상자 / 새로고침 대화상자 / 네트워크 오류 / 기타)
  → 후보 검출(YOLO 아이콘 박스 ∪ OCR 상품명 행)
  → 특징 추출("구매" 버튼 행간으로 행 구조, 아이콘 색상 서명)
  → OCR(PP-OCRv5)
  → 다중 신호 융합(텍스트 + 아이콘 방증 + 가격 유일 일치)
  → 필터(품절, 처리된 행, 유형 스위치)
  → 의사결정(BotEngine 전통 / AiBotEngine AI)
  → 최종 클릭 게이트(ClickGate: 좌표 유효, 세션 중지 아님) → 제스처
```

- **계층 분리**: 인식 계층은 "무엇이 보이는가", 판단 계층은 "무엇을 할까", 장치 계층은 "화면을 가져오고 누르기"만 담당합니다.
- **AI 파이프라인은 대체 동작하지 않습니다**. 확신이 없으면 보고하고 움직이지 않습니다.
- **ACTION → COMMIT**: 상태를 바꾸는 동작(구매, 새로고침, 예산 차감)은 **동작이 일어난 순간** 기록되며, 검증은 이후 판단에만 영향을 줍니다.
- 네이티브 C++가 ncnn 추론을 담당하며, 모델은 `assets/e7sa_yolo.ncnn.meta`로 자기 서술됩니다.

## 설치와 사용

**요구 사항**: Android 11(API 30) 이상, arm64-v8a 또는 armeabi-v7a.
실기기 검증은 OnePlus PLR110 / Android 16 / ColorOS만 해당하며, 다른 기기와 제조사 ROM은 미검증입니다.

1. 이 저장소의 Releases에서 APK를 설치합니다.
2. 앱을 열고 **접근성 서비스**를 활성화합니다(스크린샷과 제스처에 필수).
3. 선택: adb로 `WRITE_SECURE_SETTINGS`를 부여하면 원탭 활성화를 쓸 수 있습니다:
   `adb shell pm grant com.e7.shop android.permission.WRITE_SECURE_SETTINGS`
4. 위험 확인 대화상자에 동의한 뒤 『에픽세븐』 비밀 상점을 열고 "시작"을 누릅니다.
5. 먼저 **골드 상한**과 **하늘석 예산**을 설정하세요(0 = 무제한).

**중지**: 플로팅 패널 또는 앱의 "중지". 다시 "시작"하면 이전 세션을 종료하고 통계를 초기화한 뒤 현재 화면에서 재개합니다.

**복구**: 오랫동안 아무 동작이 없으면 접근성 서비스가 켜져 있는지 확인하세요(앱 업데이트 후 OS가 끌 수 있습니다).
앱은 일반적인 경우를 자가 복구합니다(무효 탭 → 백오프 재시도, 잔여 대화상자 → BACK 전송).

**문제 보고**: 아래 "버그 보고" 참조.

## 보안과 개인정보

| 항목 | 내용 |
|---|---|
| 권한 용도 | 접근성: 스크린샷과 제스처. 오버레이: 작업 패널. 알림: 상시 알림. `WRITE_SECURE_SETTINGS`(선택): 원탭 활성화. Shizuku(선택): 대체 부여 경로 |
| 화면·인식 데이터 | **기기 내에서만 처리**, 업로드하지 않음 |
| 네트워크 | **WebDAV 동기화를 설정한 경우에만**. 설정하지 않으면 네트워크 요청이 전혀 없습니다. 텔레메트리·분석 SDK 없음 |
| 저장 데이터 | 앱 설정, 실행 로그와 통계(앱 전용 영역). WebDAV 계정과 앱 비밀번호는 별도 저장하며 **시스템 백업과 기기 이전에서 제외** |
| 로그 | 인식된 화면 텍스트(게임 내 텍스트)를 포함할 수 있습니다. 공유 전에 확인하세요 |
| 데이터 삭제 | 앱 삭제 시 로컬 데이터가 모두 제거됩니다. 설정에서 통계와 로그를 지울 수 있습니다 |
| 테스트 버전 위험 | **첫 공개 버전**이며 장시간 동작은 미검증입니다. 개인정보 처리 방식은 버전마다 달라질 수 있으니 해당 릴리스 노트를 확인하세요 |

## 알려진 문제(Known Issues)

전체 목록: [docs/TEST_REPORT.md](docs/TEST_REPORT.md) (RT-001 ~ RT-014).

- **RT-004 / RT-005**: 네이티브 계층 크래시와 모델 로드 감지는 수정되었으나 **실기기 재현 검증은 미실시**.
- **RT-009**: 앱 업데이트 후 OS가 접근성 활성화를 해제합니다(다시 켜야 함 / 플랫폼 동작).
- **RT-010**: 두 테마의 레이아웃 코드에 중복이 남아 있습니다(기술 부채, 기능 영향 없음).
- **RT-012**: 인식 정확도는 모델과 화면 조건에 좌우됩니다(극단적 해상도, UI 배율, 가림, 애니메이션 중간 프레임).
- **RT-013**: 자동 조작은 **이용약관을 위반할 수 있으며** 계정 위험이 있습니다.
- **RT-014**: 접근성 서비스가 필요하고, 원탭 경로는 높은 권한을 요구합니다.

## 테스트 상태(Testing Status)

- **완료**: 단위 테스트 115/115, 두 파이프라인의 상태 머신 테스트, 적대적(레드팀) 테스트 5건,
  Android Lint(베이스라인 + 신규 시 실패), 실기기 설치/실행/UI/접근성 확인, 실제 클라우드로 WebDAV 업로드·다운로드.
- **미완료**: 밤샘 연속 실행, 다기기·다해상도, 권한 취소와 서비스 회수, 실제 네트워크 단절, 장시간 메모리·배터리, 전 언어 화면별 육안 확인.
- **결론**: **부분 테스트됨**. 시뮬레이션 계층 결과는 신뢰할 수 있으나 실기기 장시간 동작은 알 수 없습니다.
  **무인 사용은 권장하지 않습니다.**

## 버그 보고와 보안

- **일반 버그**: [Bug Report 템플릿](.github/ISSUE_TEMPLATE/bug_report.yml)을 사용하고 버전, 기기와 Android 버전, 재현 절차, 기대·실제 동작을 적어 주세요.
- **공개 이슈에 쓰지 마세요**: 계정, 비밀번호, 기기 일련번호, 개인 경로, 개인정보가 담긴 로그나 스크린샷.
- **보안 문제**(권한 우회, 데이터 유출, 임의 파일 접근, 제3자 악용 등): 공개 이슈 대신
  [SECURITY.md](.github/SECURITY.md)에 따라 비공개로 보고해 주세요.

## 로드맵

- **In Progress**: GitHub Actions(빌드 + 단위 테스트 + lint) 실제 저장소 검증, Gradle Wrapper 추가.
- **Planned**: 테마 레이아웃 중복 제거, 네이티브 크래시 경로 실기기 재현, 인식 계층 엔드투엔드 자동화, 지원 기기 확대.
- **Experimental**: 슬립 모드, 사용자 배경/타이틀 이미지, 전환 이징 선택.

## 변경 기록

- 최신 버전과 다운로드: [GitHub Releases](../../releases)
- 전체 기록: [CHANGELOG.md](CHANGELOG.md)

## 라이선스

**GNU Affero General Public License v3.0(AGPL-3.0)** 으로 배포됩니다(전문은 [LICENSE](LICENSE)).

- 사용·수정·재배포, 그리고 **상업적 이용도 허용**됩니다;
- **조건**: 파생 저작물(네트워크 서비스 포함)도 AGPL-3.0으로 라이선스하고 **사용자에게 전체 소스 코드를 제공**해야 합니다. 따라서 "이름만 바꾼 폐쇄형 유료 버전"은 허용되지 않지만, 오픈소스 상태의 유료 배포는 가능합니다.
- 서드파티 구성 요소는 각자의 라이선스를 따릅니다 — [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).
- **프로젝트 이름, 아이콘, 문서 내 도해는 코드 라이선스 범위가 아닙니다**. 『에픽세븐』 명칭과 소재는 Smilegate에 귀속됩니다.
- 내장 검출 모델은 Ultralytics 도구 체인으로 학습되었습니다(AGPL-3.0). 프로젝트 전체를 AGPL-3.0으로 공개하는 것이 무료 사용 조건을 충족합니다.

Copyright © 2026 heyyo
